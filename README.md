# Phone Detection System using YOLO model

# Set up
- Inside phone_detection_web/ install the dependencies using
```python
pip install -r requirements.txt
python detect.py
```
- Open the link shown in terminal in browser
